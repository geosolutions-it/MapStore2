

/**
 * Export all converters in a single object
 * @name converters
 * @memberof utils.Filter
 * @type {object}
 * @prop {function} toCql
 * @prop {function} toOgc
 */
import * as cql from './cql';

export default {
    cql
};
