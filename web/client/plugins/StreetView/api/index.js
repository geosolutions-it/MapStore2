import * as google from '../api/gMaps';
import * as cyclomedia from '../api/cyclomedia';

/**
 * Must Implement
 * getAPI()
 * loadAPI(apiKey)
 * getLocation(latlng)
 */
export default {
    google,
    cyclomedia
};
